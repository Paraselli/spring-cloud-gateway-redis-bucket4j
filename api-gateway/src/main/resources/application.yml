server:
  port: 8080

spring:
  application:
    name: api-gateway

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.example.gateway: INFO
    org.springframework.cloud.gateway: INFO
